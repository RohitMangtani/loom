{
  "name": "hive",
  "private": true,
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "bash apps/daemon/scripts/start.sh",
    "dev:daemon": "npm -w apps/daemon run dev",
    "dev:dashboard": "npm -w apps/dashboard run dev",
    "build": "turbo build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "turbo": "^2.8.12"
  }
}
